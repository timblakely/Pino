#include <stdio.h>

namespace bldc {
namespace firmware {
namespace platform {
void Setup() { printf("Setting up bldc version with ERRATA"); }
}  // namespace platform
}  // namespace firmware
}  // namespace bldc